{{!
	Expects parameter from the PivotWidget
	Available properties / objects:

	`#obj`
	Current Object (objectContainerTrait) thanks to the dashboard

	`#pivotTypes`
	All available pivot types with their respective collection. Pivots
	have a `label`, `ident`, `active` and `collection` property.

	This template uses the pivot-preview partial to display
	every pivot in the given ui.
}}
<div class="row">
	<section class="o-section">
		<ul class="nav nav-tabs">
			{{#pivotTypes}}
				<li role="presentation"{{#active}} class="active"{{/active}}><a href="#{{ident}}" aria-controls="{{ident}}" role="tab" data-toggle="tab">{{label}}</a></li>
			{{/pivotTypes}}
		</ul>

		<div class="tab-content js-pivots-manager">
			{{#pivotTypes}}
			<div role="tabpanel" class="tab-pane{{#active}} active{{/active}}" id="{{ident}}">
				<div class="row js-pivot-sortable" style="min-height:100px;">
				{{#table}}
					{{>charcoal/admin/widget/table-grid}}
				{{/table}}
				</div>
				<div class="col-xs-6 col-md-2">
					<a href="{{baseUrl}}admin/object/edit?obj_type={{val}}" data-type="{{val}}" data-title="{{label}}" class="js-add-pivot" target="_blank">
						<span class="glyphicon glyphicon-plus"></span>
						<span class="glyphicon-class">{{label}}</span>
					</a>
				</div>
			</div>
			{{/pivotTypes}}
		</div>
	</section>
</div>
